Your goal is to update any vulnerable dependencies in the project.

Do the foillowing

1. Run `npm audit` to find vulnerable installed packagesin this project.
2. Run `npm audit fix` to apply updates.
3. Run tests and verify the updates did not break anything.
